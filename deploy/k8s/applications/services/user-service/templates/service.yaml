apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: {{ .Values.namespace }}
spec:
  selector: { app: user-service }
  ports:
    - name: grpc
      port: 5002
      targetPort: 5002
  type: ClusterIP
