# ============================================================
# user-service ConfigMap - 非敏感环境变量
# ============================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: user-service-config
  namespace: default
data:
  USER_PORT: "5002"
  USER_ACCESS_TTL_SECONDS: "3600"
  USER_REFRESH_TTL_SECONDS: "2592000"
  USER_QUEUE_NAME: email
  USER_WORKER_CONCURRENCY: "20"
  LOG_SERVICE_NAME: user-service