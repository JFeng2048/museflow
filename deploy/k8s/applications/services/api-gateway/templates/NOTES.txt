api-gateway 已成功部署到 namespace {{ .Values.namespace }}。

Service:
  api-gateway:5001

查看状态：
  kubectl --kubeconfig=<你的 kubeconfig 路径> get pods,svc,deploy -n {{ .Values.namespace }}

查看日志：
  kubectl --kubeconfig=<你的 kubeconfig 路径> logs -n {{ .Values.namespace }} deployment/api-gateway --tail=100

API Gateway 集群内地址：
  http://api-gateway:5001
