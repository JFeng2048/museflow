apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-gateway
  namespace: {{ .Values.namespace }}
spec:
  replicas: {{ .Values.apiGateway.replicaCount }}
  # 单节点集群没有多余资源做 surge：先删旧 Pod 再起新 Pod，否则新 Pod 会因资源不足
  # 一直 Pending，而旧 Pod 又不会被回收，rollout 会卡死（代价是更新期间短暂不可用）。
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels: { app: api-gateway }
  template:
    metadata:
      labels: { app: api-gateway }
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
      containers:
        - name: api-gateway
          image: "{{ .Values.apiGateway.image.repository }}:{{ .Values.apiGateway.image.tag }}"
          imagePullPolicy: {{ .Values.apiGateway.image.pullPolicy }}
          envFrom:
            - configMapRef: { name: api-gateway-config }
            - secretRef: { name: api-gateway-secret }
          ports:
            - name: http
              containerPort: 5001
          resources: {{- toYaml .Values.apiGateway.resources | nindent 12 }}
          readinessProbe:
            httpGet: { path: /health, port: 5001 }
          livenessProbe:
            httpGet: { path: /health, port: 5001 }
