apiVersion: v1
kind: Secret
metadata:
  name: config-service-secret
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
{{- range $key, $value := .Values.configService.secret }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
