# ============================================================
# PostgreSQL Secret - 敏感信息
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: museflow                  # 可根据需要修改 namespace
type: Opaque
stringData:
  POSTGRES_USER: admin
  POSTGRES_PASSWORD: admin@museflow
  POSTGRES_DB: museflow